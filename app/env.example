# Evenly App Environment Variables
# Copy this file to .env and update the values

# API URLs
# --- 404 on "Send Verification Code"? Use EVENLY BACKEND URL here, NOT the Auth Service. ---
# CORRECT (Evenly Backend - use this in the app):
EXPO_PUBLIC_EVENLY_BACKEND_URL=https://evenly-backend-374738393915.us-central1.run.app
# WRONG (Auth Service - do NOT use in app; only in evenly-backend/.env as AUTH_SERVICE_URL):
# https://nxgenaidev-auth-api-374738393915.us-central1.run.app/api/v1/auth
#
# Local backend: EXPO_PUBLIC_EVENLY_BACKEND_URL=http://localhost:3002
EXPO_PUBLIC_AUTH_SERVICE_URL=https://nxtgenaidev-auth-2.onrender.com/api/v1/auth

# Optional (dev only): If you want to force org context header even before storage initializes,
# set this and the app will send it as `x-organization-id`.
# NOTE: This must be the **auth-service organization id** (same `organization.id` you get from signup/login responses).
# EXPO_PUBLIC_ORGANIZATION_ID=696fc87397e67400b0335682

# VoAgents API Configuration
EXPO_PUBLIC_VOAGENTS_API_URL=https://agents.voagents.ai
EXPO_PUBLIC_VOAGENTS_AGENT_ID=your-agent-id-here

# App Configuration
EXPO_PUBLIC_APP_NAME=Evenly
EXPO_PUBLIC_APP_VERSION=1.0.0

# Environment
NODE_ENV=development
