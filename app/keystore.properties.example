# Copy this file to keystore.properties (in this same folder) and fill in your values.
# Do NOT commit keystore.properties (it is in .gitignore).
# This folder is the app root - it is NOT deleted when you run prebuild/clean.

# keyAlias = the name of the key inside your keystore.
# When you created the keystore you gave it an alias (e.g. "upload" or "mykey").
# To see it: keytool -list -v -keystore evenlysplit-release-key.keystore
# Look for the line "Alias name: ..." - that value is your keyAlias.

storePassword=REPLACE_WITH_YOUR_KEYSTORE_PASSWORD
keyPassword=REPLACE_WITH_YOUR_KEY_PASSWORD
keyAlias=REPLACE_WITH_YOUR_KEY_ALIAS
