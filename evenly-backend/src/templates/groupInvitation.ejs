<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>You're invited to join <%= groupName %> on Evenly</title>
  <style>
    body { 
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; 
      background: #f8fafc; 
      margin: 0; 
      padding: 20px; 
      line-height: 1.6;
    }
    .container { 
      max-width: 600px; 
      margin: 0 auto; 
      background: #ffffff; 
      padding: 40px; 
      border-radius: 12px; 
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .logo {
      font-size: 28px;
      font-weight: bold;
      color: #3b82f6;
      margin-bottom: 10px;
    }
    .button {
      display: inline-block;
      padding: 16px 32px;
      margin: 20px 0;
      background: linear-gradient(135deg, #3b82f6, #1d4ed8);
      color: #ffffff;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      font-size: 16px;
      text-align: center;
      transition: all 0.2s ease;
    }
    .button:hover {
      background: linear-gradient(135deg, #1d4ed8, #1e40af);
      transform: translateY(-1px);
    }
    .group-info {
      background: #f1f5f9;
      padding: 20px;
      border-radius: 8px;
      margin: 20px 0;
      border-left: 4px solid #3b82f6;
    }
    .group-name {
      font-size: 20px;
      font-weight: bold;
      color: #1e293b;
      margin-bottom: 5px;
    }
    .inviter {
      color: #64748b;
      font-size: 14px;
    }
    .features {
      margin: 30px 0;
    }
    .feature {
      display: flex;
      align-items: center;
      margin: 15px 0;
      color: #475569;
    }
    .feature-icon {
      width: 20px;
      height: 20px;
      margin-right: 12px;
      color: #3b82f6;
    }
    .footer { 
      margin-top: 40px; 
      font-size: 12px; 
      color: #64748b; 
      text-align: center;
      border-top: 1px solid #e2e8f0;
      padding-top: 20px;
    }
    .warning {
      background: #fef3c7;
      border: 1px solid #f59e0b;
      border-radius: 6px;
      padding: 15px;
      margin: 20px 0;
      color: #92400e;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">Evenly</div>
      <p style="color: #64748b; margin: 0;">Expense sharing made simple</p>
    </div>

    <h1 style="color: #1e293b; margin-bottom: 20px;">
      <% if (isExistingUser) { %>
        You've been invited to a group!
      <% } else { %>
        Welcome to Evenly!
      <% } %>
    </h1>

    <div class="group-info">
      <div class="group-name"><%= groupName %></div>
      <div class="inviter">Invited by <%= inviterName %></div>
    </div>

    <% if (isExistingUser) { %>
      <p style="color: #475569; font-size: 16px;">
        <strong><%= inviterName %></strong> has invited you to join the group "<strong><%= groupName %></strong>" on Evenly. 
        Click the button below to accept the invitation and start sharing expenses with your group.
      </p>
    <% } else { %>
      <p style="color: #475569; font-size: 16px;">
        <strong><%= inviterName %></strong> has invited you to join the group "<strong><%= groupName %></strong>" on Evenly. 
        Evenly makes it easy to split expenses, track who owes what, and settle up with friends, family, or colleagues.
      </p>

      <div class="features">
        <h3 style="color: #1e293b; margin-bottom: 15px;">What you can do with Evenly:</h3>
        <div class="feature">
          <span class="feature-icon">💰</span>
          <span>Split bills and expenses easily</span>
        </div>
        <div class="feature">
          <span class="feature-icon">📊</span>
          <span>Track who owes what in real-time</span>
        </div>
        <div class="feature">
          <span class="feature-icon">🤝</span>
          <span>Settle up with group members</span>
        </div>
        <div class="feature">
          <span class="feature-icon">📱</span>
          <span>Access from any device</span>
        </div>
      </div>
    <% } %>

    <div style="text-align: center;">
      <a class="button" href="<%= invitationLink %>">
        <% if (isExistingUser) { %>
          Accept Invitation
        <% } else { %>
          Join Group & Create Account
        <% } %>
      </a>
    </div>

    <% if (!isExistingUser) { %>
      <div class="warning">
        <strong>Note:</strong> If you don't have an Evenly account yet, clicking the button above will create one for you using this email address.
      </div>
    <% } %>

    <p style="color: #64748b; font-size: 14px; margin-top: 30px;">
      This invitation will expire in 7 days. If you don't want to join this group, you can simply ignore this email.
    </p>

    <div class="footer">
      <p>This email was sent by Evenly. If you have any questions, please contact us.</p>
      <p>&copy; <%= year %> Evenly. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
