<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Expense Added - Evenly</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8fafc;
        }
        .container {
            background: white;
            border-radius: 12px;
            padding: 32px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        .header {
            text-align: center;
            margin-bottom: 32px;
        }
        .logo {
            font-size: 28px;
            font-weight: bold;
            color: #6366f1;
            margin-bottom: 8px;
        }
        .title {
            font-size: 24px;
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 16px;
        }
        .subtitle {
            color: #6b7280;
            font-size: 16px;
        }
        .expense-card {
            background: #f9fafb;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 24px;
            margin: 24px 0;
        }
        .expense-title {
            font-size: 20px;
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 8px;
        }
        .expense-details {
            color: #6b7280;
            margin-bottom: 16px;
        }
        .expense-amount {
            font-size: 24px;
            font-weight: 700;
            color: #059669;
            margin-bottom: 16px;
        }
        .split-info {
            background: #eff6ff;
            border: 1px solid #dbeafe;
            border-radius: 6px;
            padding: 16px;
            margin: 16px 0;
        }
        .split-title {
            font-weight: 600;
            color: #1e40af;
            margin-bottom: 8px;
        }
        .split-amount {
            font-size: 18px;
            font-weight: 600;
            color: #1e40af;
        }
        .footer {
            text-align: center;
            margin-top: 32px;
            padding-top: 24px;
            border-top: 1px solid #e5e7eb;
            color: #6b7280;
            font-size: 14px;
        }
        .button {
            display: inline-block;
            background: #6366f1;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 500;
            margin: 16px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">Evenly</div>
            <h1 class="title">New Expense Added</h1>
            <p class="subtitle">Someone in your group has added a new expense</p>
        </div>

        <div class="expense-card">
            <div class="expense-title"><%= expense.title %></div>
            <div class="expense-details">
                <strong>Added by:</strong> <%= addedBy.name %><br>
                <strong>Group:</strong> <%= group.name %><br>
                <strong>Date:</strong> <%= new Date(expense.date).toLocaleDateString() %><br>
                <% if (expense.description) { %>
                <strong>Description:</strong> <%= expense.description %><br>
                <% } %>
                <strong>Category:</strong> <%= expense.category %>
            </div>
            <div class="expense-amount">$<%= parseFloat(expense.totalAmount).toFixed(2) %></div>
            
            <div class="split-info">
                <div class="split-title">Your share:</div>
                <div class="split-amount">$<%= parseFloat(userSplit.amount).toFixed(2) %></div>
                <p style="margin: 8px 0 0 0; color: #6b7280; font-size: 14px;">
                    You need to pay this amount to <%= addedBy.name %>
                </p>
            </div>
        </div>

        <div style="text-align: center;">
            <a href="<%= appBaseUrl %>" class="button">View in Evenly</a>
        </div>

        <div class="footer">
            <p>This is an automated notification from Evenly.</p>
            <p>If you have any questions, please contact your group administrator.</p>
        </div>
    </div>
</body>
</html>
